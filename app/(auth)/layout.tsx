// app\(auth)\layout.tsx

/**
 * Layout untuk halaman autentikasi (login, reset password).
 * Tidak menggunakan Sidebar — layout polos.
 */
export default function AuthLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
